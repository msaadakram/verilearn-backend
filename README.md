# verilearn-backend

Backend service for VeriLearn.

## Stack
- Node.js
- Express

## Run locally
1. Install dependencies
2. Configure environment variables in `.env`
3. Start the server

## Notes
- Main application entry: `src/server.js`
- Tests and scripts are available in `test/` and `scripts/`
